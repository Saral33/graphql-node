## Graphql starter file

This project contains graphql starter file with node, express and mongodb containerized with docker.

## Start

To start this project run

```bash
  docker-compose up --build -d
```
